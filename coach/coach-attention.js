// Extension boundary for Daily Needs Attention and data-health work.
(() => {
  window.FuelGuardCoachPlatform?.registerFeature({
    id: "attention",
    host: "dashboard",
    order: 100
  });
})();
