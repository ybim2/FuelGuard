// Extension boundary for staff permissions, teams, and saved groups.
(() => {
  window.FuelGuardCoachPlatform?.registerFeature({
    id: "team-structure",
    host: "settings",
    order: 500
  });
})();
